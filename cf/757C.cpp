#include<iostream>
#include<unordered_map>
using namespace std;

int cnt[500005];

int main(){
    ios_base::sync_with_stdio(0);
    int n,m;cin>>n>>m;
    while(n--){
        int g;cin>>g;
        unordered_map<int,int> m;
        while(g--){
            int t;cin>>t;
            m[t].push_back();
        }
        for(auto &i:m){

        }
    }
}
