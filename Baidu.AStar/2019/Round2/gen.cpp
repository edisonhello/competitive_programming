#include <bits/stdc++.h>
using namespace std;


int main() {
    srand(time(0) * clock());
    int t = 2; cout << t << endl; 
    while (t--) {
        int n = rand() % 8 + 1;
        vector<int> v(n); iota(v.begin(), v.end(), 1);
        random_shuffle(v.begin(), v.end());
        int 
    }
}
