#include<bits/stdc++.h>
using namespace std;

vector<int> in,frt,bak;


int main(){
    ios_base::sync_with_stdio(0);
    int t;cin>>t;
    while(t--){
        int n,k,l;cin>>n>>k>>l;

        in.push_back(0);
        for(int i=0;i<n;i++){
            int t;cin>>t;in.push_back(t);
        }

        sort(in.begin(),in.end());

        int pw=k;
        frt.push_back(0);
        for(int i=0;i<n;i++){

        }
    }
}
