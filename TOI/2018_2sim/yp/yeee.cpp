#include <bits/stdc++.h>
using namespace std;

int main ()
{
    /*
    freopen("out.txt","w",stdout);
    int n = 1000;
    for (int i=1;n>=i;i++)
    {
        int tmp = i * (i+1) / 2;
        for (int j=1;tmp>=j;j++)
        {
            if (tmp%j == 0) printf("%d %d %d\n",i,j,tmp/j);
        }
    }
    */
    cout << int(' ') << " " << int('\n') << endl;
}
